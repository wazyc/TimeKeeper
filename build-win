const builder = require('electron-builder');

builder.build({
    config: {
        'appId': 'timekeeper',
        'win':{
            'icon': 'build/icon.ico',
            'target': {
                'target': 'zip',
                'arch': [
                    'x64',
                    'ia32',
                ],
            },

        }
    }
});